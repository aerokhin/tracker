import { TrackerValidatorGuard } from './tracker-validator.guard';

describe('TrackerValidatorGuard', () => {
  it('should be defined', () => {
    expect(new TrackerValidatorGuard()).toBeDefined();
  });
});
